apiVersion: v1
kind: Secret
metadata:
  name: news-agent-secrets
  namespace: news-agent
type: Opaque
stringData:
  # Fill these in, then apply:
  # kubectl apply -f k8s/secret.yaml
  NEWS_API_KEY: "REPLACE_ME"
  HF_TOKEN: "REPLACE_ME"
